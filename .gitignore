# Root-level ignores. Each app (website/, backend/, ml-model/) also has its
# own .gitignore for framework-specific paths.

node_modules/
.DS_Store
*.log
.env
.env.local

# ml-model: large weights and most dataset content are ignored per
# ml-model/.gitignore (crackforest/ and combined_crack/ are the exception —
# see ml-model/datasets/README.md).
